#include<stdio.h>
void main()
{
int i, n, sum=0;
printf("enter the terminating odd number: ");
scanf("%d",&n);
for(i=1;i<=n;i=i+2){
sum = sum + (i*i);
}
printf("sum of squares of odd numbers till n is: %d",sum);
}
